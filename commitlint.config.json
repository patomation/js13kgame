{
  "extends": [
    "@commitlint/config-conventional"
  ],
  "rules": {
    "scope-enum": [2, "always", [
      "docs",
      "example",
      "render",
      "snabbdom",
      "store",
      "typescript",
      "canvas",
      "view"
    ]]
  }
}

