{
  "extends": [
    "@commitlint/config-conventional"
  ],
  "rules": {
    "scope-enum": [2, "always", [
      "components",
      "docs",
      "example",
      "inventory",
      "render",
      "snabbdom",
      "store",
      "typescript",
      "canvas",
      "view"
    ]]
  }
}

