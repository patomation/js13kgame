{
  "extends": [
    "@commitlint/config-conventional"
  ],
  "rules": {
    "scope-empty": [2, "never"],
    "scope-enum": [2, "always", [
      "components",
      "dialogue-box",
      "docs",
      "example",
      "inventory",
      "render",
      "snabbdom",
      "store",
      "tools",
      "typescript",
      "canvas",
      "view"
    ]]
  }
}

